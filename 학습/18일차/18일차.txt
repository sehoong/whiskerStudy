* 구조를 어떻게 하면 잘 만들 수 있을까?
- 구조를 분리해서 사용해도 다른 컨텐츠들에 영향을 미치지 않는 구조
* css- class에 v2,v3,v4,여러개 사용해서 만드는 것은 좋지 않다.
  기존 클라스에 속성을 받으면서 한두가지 버전이 아닌 그 이상의 버전을 만들게 되면 
  기존 속성마저 바꾸는 속성을 추가하게 된다.
* 붕어빵을 끝까지 만들지 말라.
  붕어빵에 앙꼬뺴고 반죽으로 틀만 만들고
  나중에 손님들이 찾는 앙꼬를 여러개 넣을 수 있도록
* 디자인을 받았을떄, 작업시작하기전에 먼저 페이지들의 공통점을 찾는게 좋다. ex) bul-tit

데이터타입
*배열 - 데이터 순서가 상관이 없으면 배열로 불러야함.
 var arr = [1,2,3,4,56,7,8,9,1];
집단에서 누군가를 찾을려면 이름이 있어야하는데, 집단 전부에 이름이 없음.
하나하나 세야함/ 0번부터셈
ex) arr[0]; arr[1]; arr[2]; arr[3];
*객체  - 반대로 데이터 순서가 정해져있으면, 객체로 불러야함.
 {} 이름이 정해져있음.
 var obj ={
	name : '세훈',
             age : 29,
}
obj.name //'세훈'

* 'string' 문자 2
* 숫자
'2' == 2 //true   ==는 데이터 타입까지 같은지는 안따짐/ 하지만 ===은 따짐
'2' === 2 //false 

* null 값이 없다 (의도적으로)
var value;
console.log(value); //undefined  value라고는 썻지만, 정의를 하지 않았음.

undefined

*   '' == false 는 같습니다.  
'fdfdfd'.length == true /   문자가 아무것도 없어서 
''.length == false  
0 == false // true
true
false
 

